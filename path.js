// MODULO PATH Metodo que nos dice que si estamos en windows o en otro sistema operativo 

// Metodo join : permite unir directorios o rutas de directorios 
const FilPath = require("path")   
console.log(path.join);

// Metodo basename : permite extraer una URL
console.log(path.basename); 

// Metodo dirnae : para dar la ruta de los directorios o carpetas 
console.log(path.dirname);

// Metodo parse : es igual al de arriba pero en objetos 
console.log(path.parse);