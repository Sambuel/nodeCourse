const math = require("./math/math")

//console.log(math);


console.log(math.add(10,20));
console.log(math.multiply(10,20));
// Con el punto osea el (.add) estamos especificando que se ejecute solo esa funcion por ejemplo en la carpeta math creamos un archivo math con 4 funciones sumar , restar , multipliclar y dividir Al momento de querer exportar las funciones y ejemplo solo queremos ejecutar la de sumar y multiplicar ocupamos hacer el ejemplo de arriba 
